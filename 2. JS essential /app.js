    
// var let const     
    
// const temp = 10;
// temp = 20; // This will throw an error because 'temp' is a constant and cannot be reassigned.

// const temp = 100;
// console.log(temp); 

// let temp = 100;
// temp = 200; // This is valid because 'temp' is declared with 'let' and can be reassigned.
// console.log(temp) ;

// var temp = 100;
// temp = 200; // This is valid because 'temp' is declared with 'var' and can be reassigned.
// console.log(temp);  

// console.log(temp);
// var temp = 100; // This will throw a error undefined because 'temp' is accessed before its declaration due to hoisting.

// console.log(temp);
// let temp = 100; // ReferenceError: Cannot access 'temp' before initialization

// sum of two integer numbers
let a = 10;
let b = 20;
console.log("sum of " +a+ " + " +b+ " is " + (a+b));
